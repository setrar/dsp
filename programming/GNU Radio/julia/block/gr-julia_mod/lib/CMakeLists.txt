# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-julia_mod
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

# Get JULIA_HOME from environment variable
if(NOT DEFINED ENV{JULIA_HOME})
    message(FATAL_ERROR "JULIA_HOME environment variable is not set!")
else()
    set(JULIA_HOME $ENV{JULIA_HOME})
endif()

# Include directories for Julia
set(JULIA_INCLUDE_DIRS "${JULIA_HOME}/include/julia")
include_directories(${JULIA_INCLUDE_DIRS})

# Library directory for Julia
set(JULIA_LIBRARY "${JULIA_HOME}/lib/libjulia.dylib")
link_directories(${JULIA_HOME}/lib)

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX

list(APPEND julia_mod_sources
    julia_block_impl.cc)

set(julia_mod_sources
    "${julia_mod_sources}"
    PARENT_SCOPE)
if(NOT julia_mod_sources)
    message(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT julia_mod_sources)

add_library(gnuradio-julia_mod SHARED ${julia_mod_sources})
target_link_libraries(gnuradio-julia_mod
        gnuradio::gnuradio-runtime
        ${JULIA_LIBRARY}
)
target_include_directories(
    gnuradio-julia_mod
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>)
set_target_properties(gnuradio-julia_mod PROPERTIES DEFINE_SYMBOL "gnuradio_julia_mod_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-julia_mod PROPERTIES INSTALL_NAME_DIR
                                                    "${CMAKE_INSTALL_PREFIX}/lib")
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
gr_library_foo(gnuradio-julia_mod)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_julia_mod_sources)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-julia_mod)

if(NOT test_julia_mod_sources)
    message(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_julia_mod_sources)

foreach(qa_file ${test_julia_mod_sources})
    gr_add_cpp_test("julia_mod_${qa_file}" ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file})
endforeach(qa_file)
